---
import { Heading } from "../ui/heading";

interface Props {
	title: string;
	subtitle?: string;
}

const { title, subtitle } = Astro.props;
---

<section class="clip bg-gray-50 pt-14 pb-8 dark:bg-secondary-400">
	<div class="container mx-auto px-4">
		<div class="mx-auto max-w-3xl text-center">
			<Heading level="h1" gradient shadow className="mb-4 text-4xl md:text-5xl" client:idle>
				{title}
			</Heading>
			{subtitle && <p class="text-xl text-gray-600 dark:text-secondary-900">{subtitle}</p>}
		</div>
	</div>
</section>
